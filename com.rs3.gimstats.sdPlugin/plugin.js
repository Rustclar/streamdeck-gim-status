// plugin.js - Stream Deck RS3 GIM Stats logic
// Placeholder: fetch and display logic goes here
$SD.onConnected(({ actionInfo, appInfo, connection, messageType, port, uuid }) => {
  console.log("Plugin connected");
});
